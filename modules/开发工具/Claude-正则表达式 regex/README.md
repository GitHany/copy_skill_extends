# Claude Skills-regex 命令文档

regex 正则表达式模块 - 正则表达式生成和测试助手。

## 📚 目录

- [regex 正则表达式](#regex-正则表达式)

---

## regex 正则表达式

正则表达式生成和测试助手

**基础用法**:
```
/regex %{需求}%
```

**扩展示例**:

| 场景 | 命令 | 说明 |
|------|------|------|
| 邮箱验证 | `/regex 生成邮箱验证正则表达式` | 需求：生成邮箱验证正则表达式 |
| 手机号验证 | `/regex 生成中国手机号验证正则表达式` | 需求：生成中国手机号验证正则表达式 |
| URL 匹配 | `/regex 生成 URL 匹配正则表达式` | 需求：生成 URL 匹配正则表达式 |
| 密码强度 | `/regex 生成密码强度校验正则表达式` | 需求：生成密码强度校验正则表达式 |
| 日期格式 | `/regex 生成日期格式验证正则表达式` | 需求：生成日期格式验证正则表达式 |
| IP 地址 | `/regex 生成 IPv4 地址验证正则表达式` | 需求：生成 IPv4 地址验证正则表达式 |
| 身份证号 | `/regex 生成中国身份证号验证正则表达式` | 需求：生成中国身份证号验证正则表达式 |
| HTML 标签 | `/regex 生成 HTML 标签提取正则表达式` | 需求：生成 HTML 标签提取正则表达式 |